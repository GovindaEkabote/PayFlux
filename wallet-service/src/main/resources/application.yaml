spring:
  application:
    name: wallet-service

  datasource:
    url: jdbc:mysql://localhost:3306/payflux_wallet
    username: root
    password: Govinda@1997
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 5006

